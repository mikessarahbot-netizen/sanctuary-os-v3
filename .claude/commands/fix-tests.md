# /fix-tests

Fix failing tests. Narrowest fix only.

1. Read failing test output
2. Determine: bug in code, test assumptions, or missing setup
3. Fix only what is broken
4. Do not weaken assertions without explanation
